/**
 * @param {import('markdown-it')} md
 */
module.exports = function (md, option) {
    // md.block.ruler.after('heading', 'anchor', function(state, start, end, silent) {
    //     const last = state.tokens[state.tokens.length - 1];
    //     if(last.type === 'heading_close') {
    //         console.log(last);
    //     }
    // });
};
